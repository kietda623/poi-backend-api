<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="AppUser.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:AppUser.Pages"
    Shell.BackgroundColor="{StaticResource Primary}"
    Shell.ForegroundColor="White"
    Shell.TitleColor="White"
    Shell.UnselectedColor="#AAFFFFFF"
    Shell.TabBarBackgroundColor="{StaticResource Surface}"
    Shell.TabBarForegroundColor="{StaticResource Primary}"
    Shell.TabBarUnselectedColor="{StaticResource TextHint}">

    <ShellContent
        x:Name="LoginContent"
        Route="login"
        ContentTemplate="{DataTemplate pages:LoginPage}"
        Shell.TabBarIsVisible="False" />

    <TabBar x:Name="MainTabBar">
        <ShellContent
            x:Name="HomeTab"
            Route="home"
            Title="Trang chủ"
            ContentTemplate="{DataTemplate pages:HomePage}">
            <ShellContent.Icon>
                <FontImageSource FontFamily="{x:Null}" Glyph="🏠" Size="20" />
            </ShellContent.Icon>
        </ShellContent>

        <ShellContent
            x:Name="ExploreTab"
            Route="poiList"
            Title="Khám phá"
            ContentTemplate="{DataTemplate pages:POIListPage}">
            <ShellContent.Icon>
                <FontImageSource FontFamily="{x:Null}" Glyph="🔍" Size="20" />
            </ShellContent.Icon>
        </ShellContent>

        <ShellContent
            x:Name="ChatTab"
            Route="chat"
            Title="Chatbot"
            ContentTemplate="{DataTemplate pages:ChatPage}">
            <ShellContent.Icon>
                <FontImageSource FontFamily="{x:Null}" Glyph="🤖" Size="20" />
            </ShellContent.Icon>
        </ShellContent>

        <ShellContent
            x:Name="TourPlanTab"
            Route="tourPlan"
            Title="Tour Plan"
            ContentTemplate="{DataTemplate pages:TourPlanPage}">
            <ShellContent.Icon>
                <FontImageSource FontFamily="{x:Null}" Glyph="🗺️" Size="20" />
            </ShellContent.Icon>
        </ShellContent>

        <ShellContent
            x:Name="ProfileTab"
            Route="profile"
            Title="Hồ sơ"
            ContentTemplate="{DataTemplate pages:ProfilePage}">
            <ShellContent.Icon>
                <FontImageSource FontFamily="{x:Null}" Glyph="👤" Size="20" />
            </ShellContent.Icon>
        </ShellContent>
    </TabBar>

</Shell>
